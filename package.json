{
  "name": "ripple-client-lib",
  "version": "2.0.0",
  "description": "wrapped ripple-lib and did abstract, do convenience",
  "repository": "ripple-client-lib",
  "main": "index.js",
  "scripts": {
    "test": "jest --env node --detectOpenHandles",
    "watch": "jest --watchAll --env node --detectOpenHandles"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "ripple-lib": "1.2.3"
  },
  "devDependencies": {
    "jest": "^24.7.1",
    "prettier": "^1.17.0"
  }
}
